/vendor/

# A library does not commit its lock file: consumers resolve their own versions.
/composer.lock

# Tool caches.
/.phpunit.cache/
/.phpunit.result.cache
/.php-cs-fixer.cache

# Never commit credentials.
.env
.env.*
!.env.example
